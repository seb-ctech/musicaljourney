Kulitta is a framework for automated composition that can also be configured to run as a standalone AI for generating music in a particular style. 

Some code 

```haskell 
modMajMin :: Int -> MP -> MP 
```